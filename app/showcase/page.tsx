import ShowcaseClient from './ShowcaseClient';

export const dynamic = 'force-static';

export const metadata = {
  title: 'Orbit Case Studies - Pianify Production Proof',
  description: 'Examine detailed product cases where Orbit built and shipped real features for the Pianify piano learning app.',
};

export default function ShowcasePage() {
  return <ShowcaseClient />;
}
